# Elsie.Sample.Dashboard

Multi-page dashboard — cookie auth, form CSRF, validation. Keep Views/ and wwwroot/. Seed: ada@elsie.dev / pass

Uses **NuGet** packages only — copy this folder and run (no monorepo required):

```bash
dotnet run
```

Requires .NET 8 SDK. Package versions are in `Elsie.Sample.Dashboard.csproj`.
