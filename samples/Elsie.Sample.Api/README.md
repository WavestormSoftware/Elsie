# Elsie.Sample.Api

Todos API — validation, compression, OpenAPI, API key on mutations.

Uses **NuGet** packages only — copy this folder and run (no monorepo required):

```bash
dotnet run
```

Requires .NET 8 SDK. Package versions are in `Elsie.Sample.Api.csproj`.
