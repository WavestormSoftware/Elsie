# Elsie.Sample.Views

Fluid/Liquid views + layout. Keep the Views/ folder.

Uses **NuGet** packages only — copy this folder and run (no monorepo required):

```bash
dotnet run
```

Requires .NET 8 SDK. Package versions are in `Elsie.Sample.Views.csproj`.
