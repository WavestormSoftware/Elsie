# Elsie.Sample.Hello

DI, typed route/query, constraints, pipelines.

Uses **NuGet** packages only — copy this folder and run (no monorepo required):

```bash
dotnet run
```

Requires .NET 8 SDK. Package versions are in `Elsie.Sample.Hello.csproj`.
